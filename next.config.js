/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['fotosretiroanas2022.s3.us-east-2.amazonaws.com', "via.placeholder.com", "localhost", "resgatando-anas.vercel.app"],
      }
}

module.exports = nextConfig
